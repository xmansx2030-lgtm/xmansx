# PHASE 12 REPORT — Student Actions, Commitments & Official Documents

**التاريخ:** 2026-08-20

## Status

`COMPLETE` — كل معايير القبول (البند 154) تحققت بتشغيل فعلي موثق أدناه. لا بند NOT RUN.

## StudentAction architecture

تطبيق `student_actions/` مستقل عن `documents/` وعن `student_warnings/` — ثلاثة
مفاهيم لا تخلط في Model واحدة (البند 1): الإنذار «ما استحقه الطالب»، والإجراء «ما
فعلته الإدارة»، والمستند «ما صدر وطُبع». الإجراء **تسجيل فقط**: لا يعدل علامة حضور
ولا ملخصًا يوميًا ولا عذرًا ولا إنذارًا ولا وصولًا صباحيًا (البند 2، واختبار يثبت
ثبات المصادر بعد التسجيل).

## Action types

سبعة أنواع Enum ثابتة (البند 5) بعرض عربي فقط: التواصل مع ولي الأمر، مقابلة الطالب،
مقابلة ولي الأمر، أخذ تعهد، تسليم إنذار، ملاحظة إدارية، إجراء آخر — اختبار مُوسَّع
يثبت الأنواع السبعة. **لم يُنشأ `StudentActionTemplate`**: قائمة الـEnum كافية لـMVP
ولا نموذج بلا استخدام (البند 12)، وإضافته لاحقًا لا تكسر البيانات.
الحالة `COMPLETED`/`CANCELLED` فقط بلا Workflow زائد (البند 7)، وقيد قاعدة بيانات
يمنع «ملغى» بلا وقت إلغاء. لا حذف نهائي: الإلغاء بسبب وفاعل ووقت، والإلغاء مرتين
يرد `STUDENT_ACTION_ALREADY_CANCELLED`.

## Warning linking

`warning` اختياري (إجراء مستقل مسموح — البند 9). عند إرساله يتحقق الخادم من المدرسة
**والطالب** معًا، وإلا `INVALID_ACTION_WARNING_LINK` — بنفس الرمز لإنذار مدرسة أخرى
حتى لا يكشف وجوده (اختباران). تسليم الإنذار لا يفترض من إصداره: إجراء صريح
(البند 70).

## GeneratedDocument

`GeneratedDocument` يحمل: النوع، القالب وإصداره، `snapshot_schema_version`، الحالة،
اللقطة، الملف وحجمه ونوعه وبصمته، `revision`، رمز الخطأ، المنشئ ووقته، وحقول
الإلغاء. `storage_key` = `file.name` (مسار عشوائي في مخزن خاص). التفصيل في
[GENERATED_DOCUMENTS.md](docs/GENERATED_DOCUMENTS.md) وERD.md.

## Snapshot design

اللقطة JSON تكفي وحدها لإعادة رسم المستند: بيانات المدرسة الرسمية (من
`SchoolSettings` لا من المستخدم الحالي — البند 80)، بيانات الطالب **برقم هوية مقنّع
فقط** (البند 19)، قيم المستند، الفترة، ومعرّف القالب. مستندات الإنذار تُبنى من لقطة
م11 **وحدها**: اختبار عدّاد استعلامات يثبت أنها لا تستعلم عن أي مقياس حضور (البند 30).
`snapshot_schema_version = 1` يدعم قراءة المستندات القديمة بعد تطور البنية (البند 20).

## Template versioning

سجل مُصدَّر (`templates_registry.py`) بثمانية قوالب `v1`، وكل مستند يخزن مفتاح
قالبه وإصداره. تعديل تصميم لاحقًا يضيف `v2` ولا يعدل `v1`، فيبقى القديم مطابقًا لما
صدر. إصدار مفقود ⇒ `DOCUMENT_TEMPLATE_VERSION_UNAVAILABLE`، ونوع بلا قالب ⇒
`DOCUMENT_TEMPLATE_NOT_FOUND`.
**سياسة النماذج الرسمية (البندان 24-25):** كل قوالب MVP `source_type = INTERNAL`
ولا توصف بأنها معتمدة وزاريًا؛ اعتماد نموذج رسمي يتطلب مدخلًا يحمل
`source_reference`/`source_version`/`effective_date`. **ولا يعدّل نموذج رسمي لإضافة
تفاصيل**: التعهد بلا جدول تواريخ والتفصيل كشف مستقل يُرفق (البند 27).

## Warning documents

من إنذار صادر (لا ملغى) يُنتج `WARNING_LEVEL_1/2/3` بدرجة مطابقة (عدم التطابق
مرفوض). المستند يعرض القيمة والحد **وقت الإصدار** وكل مقاييس اللقطة، والصباحي منفصل
عن تأخر الحصص فيه.

## Commitment documents

`ATTENDANCE_COMMITMENT` يجمّد الغياب بدون عذر وقت الإنشاء مع الفترة، ويعبئ اسم
الطالب والصف والفصل والمدرسة واسم المدير الرسمي وتاريخ الإصدار، وأماكن توقيع
الطالب وولي الأمر والمدير (بلا توقيع رقمي — البندان 81-82). إنشاؤه من الواجهة يسجل
إجراء `COMMITMENT_TAKEN` **بعد** جاهزية المستند (سياسة ثابتة — البند 69).

## Absence detail report

مدى `from_date`/`to_date`، وصفوف: التاريخ واليوم ونوع اليوم وحصص الغياب وبعذر وبدون
عذر والتصنيف. **اليوم المختلط** يعرض تصنيفه بأرقامه ولا يصنف كله خطأ (البند 35)،
و**غير المحسوم** يعرض «بيانات التحضير غير مكتملة» ولا يعد غيابًا كاملًا (البند 36) —
اختبار يغطي الحالات الأربع بترتيبها.

## Morning late report

مصدره `SchoolArrival` وحده: التاريخ ووقت الوصول والتأخر الخام والمحتسب والمصدر،
وملخص بعدد المرات وإجمالي الدقائق والمدة بصيغة ساعات ودقائق. **لا يدخله
`AttendanceMark.LATE` إطلاقًا** (اختباران + E2E)، والوصول في الوقت لا يحتسب.

## Period late report

مصدره `AttendanceMark.LATE`: التاريخ والحصة ووقت الوصول ودقائق التأخر + ملخص
مستقل. اختبار يثبت أن تأخرًا صباحيًا كبيرًا لا يظهر فيه.

## Student attendance report

يجمع الغياب الكامل/الجزئي/بعذر/بدون عذر/المختلط/غير المحسوم وحصص الغياب وتصنيفها،
والتأخرين **بعدادين منفصلين**، والإنذارات، والإجراءات. **بلا أي بيانات إرشاد أو
إحالات** (البند 45، واختبار يثبت غياب المفاتيح).

## PDF engine

**WeasyPrint 69** (‏HTML+CSS → PDF). اختير بعد فحص فعلي داخل الحاوية لا بالافتراض:
يشكّل العربية ويضبط الاتجاه عبر Pango/HarfBuzz/FriBidi، ويدعم CSS الطباعة الحقيقي
(‏`@page`, ترقيم الصفحات، تكرار `thead`, منع قطع الصفوف)، ويعمل داخل Docker.
مكتبات النظام أضيفت في `backend/Dockerfile` عبر مرحلة `runtime-libs` مشتركة بين
dev وprod. الاستيراد **كسول**: مضيف تطوير بلا مكتبات GTK لا يكسر استيراد النماذج.
**سياسة الخطوط (البند 49):** حزمة `fonts-noto-core` من النظام (رخصة OFL) — لا ملفات
خطوط داخل المستودع. والتوليد الرسمي **خادمي دائمًا** (البند 47).

## Arabic / RTL

كل القوالب `<html lang="ar" dir="rtl">` بمقاس A4. تحقق فعلي على مستند مولد: النص
العربي مستخرج فعليًا بـ`pypdf` (لا مربعات ولا OCR)، الأرقام والجداول سليمة، ومستند
120 صفًا أنتج 3+ صفحات برؤوس أعمدة متكررة وبلا قطع صفوف.

## Storage

الملفات في `GENERATED_DOCUMENTS_ROOT` **خارج `MEDIA_ROOT`** لأن إعدادات التطوير
تخدم `MEDIA_URL` عبر HTTP. المخزن يرفض `url()` صراحةً (اختبار يثبت رفع الاستثناء)،
والمفتاح عشوائي `school_{id}/{uuid}.pdf` بلا اسم طالب أو نوع مستند.

## Download security

التنزيل عبر endpoint مصادق مقيّد بالمستأجر والدور و`READY` فقط: المدير والوكيل
ينزلان، **المرشد يرى البيانات الوصفية ولا ينزل** (البند 85)، والمعلم محجوب عن كل
شيء. `can_download` في الاستجابة يخفي الزر في الواجهة. مستند مدرسة أخرى ⇒ 404 على
كل المسارات. حدث `DOCUMENT_DOWNLOADED` يسجل بلا محتوى.

## Retry / reprint behavior

- إعادة الطباعة = **الملف المخزن نفسه** (لا توليد جديد): تنزيلان قبل/بعد تغير
  المقاييس أعطيا بايتات متطابقة وبصمة واحدة (اختبار + E2E).
- `retry` للمستندات `FAILED` فقط ويعيد الرسم من **اللقطة المخزنة**: اختبار يغير
  اسم الطالب وغيابه بين المحاولتين ويثبت أن الناتج لم يتغير.
- ملف مفقود ⇒ `DOCUMENT_FILE_MISSING` وحالة المستند لا تُزوَّر — لا توليد صامت من
  البيانات الحالية (البند 60).

## Student profile integration

تبويبان جديدان: **الإجراءات** (التاريخ/النوع/المنفذ/الإنذار المرتبط/الملاحظة/الحالة
+ إضافة وإلغاء) و**المستندات** (النوع/التاريخ/المنشئ/النسخة/الحالة + معاينة قبل
الإصدار + عرض/إعادة طباعة + إعادة محاولة + إلغاء للمدير). المعاينة تعرض ملخص
البيانات وتنبه إن وُجد مستند أصلي لنفس الإنذار.

## Permissions

مصفوفة PERMISSIONS.md محدّثة. اختبارات API تثبت: المرشد يقرأ ولا يكتب ولا ينزل،
المعلم محجوب عن القوائم والتنزيل، الوكيل ينشئ ولا يلغي مستندًا، والمدير وحده يلغي.

## Audit

`STUDENT_ACTION_CREATED/CANCELLED` و`DOCUMENT_GENERATION_REQUESTED/GENERATED/
GENERATION_FAILED/VOIDED/DOWNLOADED` — معرفات وأنواع وأحجام فقط: **لا محتوى PDF ولا
لقطة كاملة ولا نص ملاحظة ولا اسم طالب** (البند 94).

## Purge

خطوتان جديدتان بالترتيب الملزم: «المستندات المولدة» ← «الإجراءات الطلابية» ←
«إنذارات الطالب» (تبعيات PROTECT)، واختبار يثبت الترتيب فعليًا. ملفات PDF مسجلة في
`PURGE_STORAGE_COLLECTORS` واختبار يثبت التقاطها. القوالب بيانات نظام فلا تحذف.

## Phase 13 readiness

سجل الإجراءات كامل ومرتب زمنيًا لكل طالب مع ربط الإنذارات، فتستهلكه الإحالة للمرشد
(م13) دليلًا على ما جُرِّب — بلا إعادة بناء وبلا تعديل بنية.

## Backend tests

**495/495 PASS** داخل الحاوية (‏432 انحدار للمراحل 1→11 + **63 جديدة**: 16 إجراءات +
31 مستندات + 15 API + اختبار الاقتطاع) و**0 تخطٍّ**. التغطية: الأنواع السبعة، ربط الإنذار وعزله،
التاريخ المستقبلي، الإلغاء ومنع تكراره، عدم مس المصادر، الترتيب الزمني؛ PDF حقيقي
وبصمته، النص العربي المستخرج، تجميد اللقطة عبر تغير المقاييس والاسم والقيد والقواعد،
عدم إعادة الحساب (عدّاد استعلامات)، التكرار وتحرير المكان بعد الإلغاء، الإنذار
الملغى وعدم تطابق الدرجة؛ التعهد وتجميده؛ تصنيفات الغياب الأربعة واليوم المختلط
والمدى المقلوب؛ التأخر الصباحي وفصله عن الحصص والوصول في الوقت؛ تقرير المواظبة بلا
إرشاد؛ 120 صفًا متعدد الصفحات، الأسماء الطويلة، تهريب الوسوم، الملف خارج MEDIA
وبلا رابط؛ الفشل وعدم ادعاء الجاهزية، إعادة المحاولة من اللقطة، رفضها للجاهز، الملف
المفقود؛ العزل والأدوار وmass assignment وPurge.
‏ruff نظيف، `manage.py check` نظيف، `check --deploy` exit 0، `makemigrations --check`
بلا تغييرات، ‏OpenAPI يولد بلا تحذير جديد من مسارات م12.

**بيئة التشغيل المعتمدة لهذه المرحلة: داخل الحاوية**
(`docker compose exec -T -e DJANGO_SETTINGS_MODULE=config.settings.test backend
python -m pytest`) لأن توليد PDF يحتاج مكتبات النظام. اختبارات المستندات تحمل حارس
تخطٍّ لمضيف بلا هذه المكتبات فقط — وفي الحاوية **لم يتخطَّ أي اختبار**.

## Frontend tests

**127/127 Vitest PASS** (‏112 انحدار + **15 جديدة**): قائمة الإجراءات بالنوع والمنفذ
والإنذار المرتبط، الإنشاء بحمولة نية فقط، الإلغاء بسبب، إخفاء الإنشاء عن المرشد؛
المعاينة قبل الإصدار وعرض قيمة الإصدار، الإنشاء بحمولة نظيفة (بلا snapshot/قيم
موثوقة)، مستند بمدى تواريخ، صف جاهز برابط تنزيل ونسخة القالب، إعادة المحاولة للفاشل
وإخفاء تنزيله، المرشد بلا تنزيل ولا نموذج، الإلغاء للمدير وحده، التعهد يرسل
`create_action`, ظهور التبويبين، وخلو الصفوف من رقم هوية كامل.
‏typecheck + lint + build نظيفة.

## E2E

`documents.spec.ts` — **5/5 PASS** ضد Docker حقيقي:
1. استيراد صف فريد (noor-9) → 5 أيام غياب → إصدار الإنذار الثاني عند 5 → معاينة
   («صدر عند 5 يوم») → إنشاء المستند من الواجهة → صف «جاهز» بنسخة `warning_level_2:v1`
   → تنزيل PDF فعلي (‏`%PDF-`, ‏`application/pdf`) → اعتماد عذر ليومين → **إعادة
   التنزيل تعطي نفس الحجم ونفس البصمة، واللقطة ما زالت 5**.
2. تعهد → PDF جاهز → إجراء «أخذ تعهد» يظهر تلقائيًا ويبقى بعد إعادة التحميل.
3. تسجيل «التواصل مع ولي الأمر» → يبقى بعد إعادة التحميل → إلغاؤه بسبب يحوّل الحالة
   إلى «ملغى» بلا حذف.
4. كشف غياب للفترة: 5 أيام غياب كامل، **2 بعذر و3 بدون عذر** داخل اللقطة، وPDF سليم.
5. ثلاث حالات تأخر صباحي يدوي → كشف صباحي بـ3 مرات ودقائق موجبة، وكشف تأخر الحصص
   **صفر** للطالب نفسه (فصل تام).
6. العزل: مديرة مدرسة أخرى — تفاصيل المستند 404، التنزيل 404، إنشاء إجراء 404،
   وقائمة المستندات صفر.

**الحزمة الكاملة: 41/41 PASS** (‏10.8 دقيقة، `workers:1` ضد Docker حقيقي، **بلا أي
إعادة محاولة**) — 36 اختبار انحدار للمراحل 1→11 + 5 للمرحلة 12.

### خللان حقيقيان كشفهما التشغيل الكامل (وأُصلحا)

1. **قائمة «غير الموجودين في آخر ملف نور» كانت مقتطعة عند 500.** ملخص وظيفة
   الاستيراد يخزن `missing_names` مقتطعة **للعرض**، وفلتر المراجعة كان يبنى عليها —
   فأي مدرسة يتجاوز فيها المفقودون 500 تُخفى عنها طلاب فعليون ولا يمكن تصنيفهم.
   ظهر حين تجاوزت مدرسة التطوير 500 طالب وسقط طالب الاختبار خارج الاقتطاع. الإصلاح:
   `summary["missing_ids"]` قائمة معرفات **كاملة** يعتمدها الفلتر (مع fallback
   للوظائف الأقدم)، واختبار خلفي يثبت ظهور طالب خارج حد الاقتطاع.
2. **صفحة فارغة في أول تحميل بعد تغيّر شجرة المصادر.** خادم التطوير (Vite) يعيد
   تجميع الاعتماديات عند أول طلب بعد إضافة ملفات مصدر جديدة، فترجع طلبات الوحدات
   ‏504 وتظهر صفحة بلا نموذج دخول — أسقط اختبارين مختلفين في تشغيلين متتاليين
   (كلاهما ينجح منفردًا). الإصلاح: تسخين الواجهة في `global-setup` قبل أول اختبار،
   ومحاولة إضافية واحدة في إعداد Playwright — والتشغيل النهائي مرّ **بلا استخدام أي
   إعادة محاولة**.

## Performance

قياس `benchmark_documents` (حاوية التطوير، البيانات تعاد كما كانت):

| العملية | الزمن |
|---|---|
| مستند إنذار | 819ms |
| تعهد حضور | 288ms |
| كشف غياب 30 صفًا | 575ms |
| كشف غياب 100 صف | 1.9s |
| كشف غياب 300 صف | 4.0s |
| رسم كشف 500 صف (22 صفحة، 139KB) | 6.2s |
| بناء لقطة 300 صف (بلا PDF) | 18ms |
| قائمة المستندات (25) | 8ms — **استعلام واحد** |
| قائمة الإجراءات (25) | 11ms — **استعلام واحد** |

الاستعلامات ليست العنق (لا N+1): الرسم ~250ms لكل صفحة A4. جرّبت تبسيط CSS (إلغاء
منع قطع الصفوف/تكرار الرؤوس) فلم يتغير الزمن يذكر، فالسبب تخطيط WeasyPrint نفسه.
لذلك ضُبط `MAX_REPORT_ROWS = 500` بالقياس (‏≈6 ثوانٍ في أسوأ حالة) بدل 2000.

## Fresh migration

قاعدة جديدة `xmansx_fresh12` → migrate كامل PASS (‏`student_actions.0001` و
`documents.0001/0002`) ثم حذفت. `makemigrations --check` بلا تغييرات.

## Docker

الخدمات الست سليمة بعد إعادة بناء صورة الخلفية (‏backend/worker/postgres/redis
healthy، ‏beat/frontend Up). لا خدمة جديدة ولا Celery في مسار المستندات (التوليد
متزامن). ‏Attendance/Excuses/Warnings/Devices/Bridge/Roster Sync/Student Profile كلها
تعمل — مثبت بالانحدار الكامل.

## Security review

- **تخزين**: خارج MEDIA_ROOT، بلا رابط عام (رفض `url()`)، مفتاح عشوائي، تنزيل
  مصادق مقيّد بالدور والمستأجر.
- **IDOR/عزل**: 404 على كل مسار لمورد مدرسة أخرى (اختبارات + E2E)، وربط إجراء
  بإنذار طالب آخر مرفوض برمز غامض.
- **Mass assignment**: حقول اللقطة/القالب/الحالة غير معرفة في الـserializers، واختبار
  حقن يثبت تجاهل `metric_value_at_issue: 99` و`snapshot_data` و`template_version`.
- **XSS/حقن HTML**: تهريب Django التلقائي (اختبار على `<script>`)، و`base_url=None`
  يمنع جلب أي مورد أو قراءة ملف (path traversal).
- **PII**: لا رقم هوية صريح في مستند أو استجابة، ولا محتوى/لقطة في السجلات.
- **تزامن**: الحكم النهائي قيد فريد جزئي في قاعدة البيانات لا فحص تطبيقي — النقر
  المزدوج يمر بنفس مسار `IntegrityError → 409` الذي يمر به طلبان متزامنان.
- **تنظيف الملفات**: الحذف النهائي يزيل ملفات PDF من التخزين فعليًا.

## Known limitations

- **التوليد متزامن داخل الطلب**: تقرير 500 صف يحجز عامل خادم ~6 ثوانٍ (مهلة العميل
  لهذا المسار رُفعت إلى 60 ثانية). النموذج يدعم `PENDING` فعليًا فالنقل إلى Celery
  تغيير محصور.
- **لا شعار مدرسة** داخل المستندات: تضمين صورة يجعل اللقطة غير مكتفية بذاتها أو
  يربطها بملف قد يتغير؛ الترويسة نصية.
- لا توقيع رقمي ولا ختم (أماكن توقيع فارغة فقط) — قرار MVP.
- تقرير المواظبة يعرض الإجراءات والإنذارات بلا ترقيم داخلي: طالب بمئات الإجراءات
  ينتج مستندًا طويلًا (حارس 500 صف يحمي الكشوف لا هذا القسم).
- استرجاع مستند فقد ملفه ليس تلقائيًا: يتطلب قرار مشغل صريح (موثق).

## Technical debt

- نقل الرسم إلى Celery مع بث حالة `PENDING → READY` للواجهة.
- تخزين كائني (S3) بروابط موقّعة قصيرة بدل نظام الملفات المحلي.
- `PERIOD_LATE_OCCURRENCES` كنوع إنذار (م11) لم ينفذ بعد، وكشفه هنا جاهز.
- تحذيرات OpenAPI القديمة (‏non-blocking، خطة OPENAPI.md).

## Files changed

Backend: إصلاح `students/services/imports/comparison.py` و`students/api/lifecycle_views.py`
(قائمة المفقودين الكاملة) + تطبيقا `student_actions/` و`documents/` كاملين (models/migrations/services/
selectors/templates/api/urls/admin/purge_integration/benchmark) + `audit/models.py`
(7 أحداث) + `config/{settings/base,urls}` + `Dockerfile` (مرحلة `runtime-libs`
ومكتبات PDF والخطوط) + `pyproject.toml` (‏weasyprint + pypdf للاختبارات) +
اختبارات (‏test_student_actions.py، ‏test_documents.py، ‏test_documents_api.py).
Frontend: `features/documents/` (‏api + StudentActionsTab + StudentDocumentsTab +
documents.test.tsx) + دمج التبويبين في `StudentAttendanceProfilePage`.
E2E: `documents.spec.ts` + `scripts/generate_e2e_fixtures.py` (‏noor-9) +
`e2e/global-setup.ts` (تسخين) + `playwright.config.ts` (محاولة إضافية واحدة).
Docs: ‏STUDENT_ACTIONS.md وGENERATED_DOCUMENTS.md وDOCUMENT_TEMPLATES.md جديدة +
تحديث ARCHITECTURE/ERD/PERMISSIONS/SECURITY/DATA_PURGE/STUDENT_WARNINGS/
ABSENCE_EXCUSES/STUDENT_ATTENDANCE_PROFILE.

## Commit

`feat: add student actions commitments and generated documents` — بلا Push.

## Ready for Phase 13?

نعم — سجل الإجراءات جاهز لتستهلكه الإحالات، والمستندات مستقلة عنها.
**متوقف بانتظار الاعتماد.**
