# PHASE 11 REPORT — Warning Rules & Student Warnings

**التاريخ:** 2026-08-19

## Status

`COMPLETE` — كل معايير القبول (البند 151) تحققت بتشغيل فعلي موثق أدناه. لا بند NOT RUN.

## Warning architecture

وحدة `student_warnings/` (‏models + services/{rules,issuance} + selectors/eligibility +
api + purge_integration + benchmark). **قرار تسمية موثق:** لا يمكن تسمية الحزمة
`warnings` لأنها تُظلّل وحدة بايثون القياسية التي يستوردها Django نفسه (‏`backend/`
جذر `sys.path`) — انحراف عن اسم ERD الاصطلاحي لسبب تقني، وERD حُدّث.

الاكتشاف مشتق بالكامل (لا صفوف تنبيه ولا Audit لكل حساب)، والإصدار سجل صريح بقرار
بشري. **لا إعادة بناء لمنطق الغياب/الأعذار**: مقياس الغياب يستهلك
`UNEXCUSED_FULL_DAY_FILTER` المستخرج من selectors المرحلة 10 (المصدر المرجعي الوحيد).

## Rule model / types / levels / thresholds

`WarningRule(school, rule_type, level, threshold, is_enabled)` —
‏UNIQUE(school, rule_type, level) + CHECK(1..200). النوعان:
‏`UNEXCUSED_FULL_DAY_ABSENCE` و`MORNING_LATE_OCCURRENCES`؛ المستويات
‏LEVEL_1/2/3 بافتراضي 3/5/10 لكل نوع (تنشأ idempotent عند أول قراءة).
**قواعد صريحة لا JSON داخل SchoolSettings.** التحقق التصاعدي إلزامي
(‏5/3/10 و3/3/10 و0/5/10 مرفوضة برموزها) و**التحديث ذري** لكل النوع — اختبار يثبت
بقاء القيم الأصلية سليمة بعد الرفض. القواعد معزولة لكل مدرسة (اختبار).

## Absence eligibility

`FULL` + `excused_absent_periods = 0` + `unexcused_absent_periods > 0` — تعريف م10
حرفيًا. المستثنى (اختبارات): بعذر، جزئي، غير محسوم، **والمختلط** (سياسة MVP موثقة).
اختبارات العتبات: 2 يوم → لا استحقاق، 3 → المستوى الأول، 5 → الثاني.

## Morning late eligibility

`SchoolArrival.status = LATE` عددًا لا دقائق. الوصول في الوقت لا يحتسب، **وتأخر
الحصص لا يدخل إطلاقًا** (اختباران + E2E). تصحيح وصول متأخر إلى «في الوقت» يخفض
القيمة الحالية فورًا (اختبار).

## Academic scope

**العام الدراسي النشط** — قرار بالأدلة لا بالتخمين: `DailyAttendanceSummary` يحمل
`academic_year` وبلا فصل، و`SchoolArrival` بلا الاثنين، و`Semester` اختياري nullable
فينكسر النطاق الفصلي صامتًا لمدارس بلا فصول. المرساة العملية: مقياس الغياب على
**FK العام في صف الملخص** (أدق من نطاق التواريخ)، ومقياس التأخر على نافذة تواريخ
العام (لا FK متاح). ‏`semester` يحفظ snapshot مرجعيًا في الإنذار.

**اكتشاف أثناء التنفيذ:** بيانات التطوير كانت تعرّف عامًا «نشطًا» يبدأ 2026-08-23
بينما اليوم 2026-08-19 — أي عام نشط لم يبدأ بعد، فتصبح كل المقاييس المرتبطة بنطاق
التواريخ فارغة. أصلح `seed_dev` ليصحح ذلك (اتساق بيئة تطوير موثق).

## StudentWarning / Snapshot

الحقول في ERD.md. الجوهر: `threshold_at_issue` و`metric_value_at_issue` +
snapshot الطالب (اسم/صف/فصل/هوية مقنعة — **لا هوية plaintext**) + مقاييس م8/م8.5/م10
كاملة، **بعدادين منفصلين** للتأخر الصباحي وتأخر الحصص. اختبارات: تغيير الحدود بعد
الإصدار، نقل الطالب، تغيير اسمه — كلها لا تمس Snapshot.

## Issuance / duplicate protection

`POST /warnings/issue/` بجسد من ثلاثة حقول فقط. الخادم: يتحقق من الدور والمستأجر
والقاعدة والتفعيل، **ويعيد حساب الاستحقاق لحظة الإصدار** (لا ثقة بالواجهة)، ثم ينشئ
داخل transaction. القيد الفريد الجزئي
(`WHERE status='ISSUED'`) هو الحكم النهائي: النقر المزدوج → إنذار واحد و409
(اختبار خلفي + اختبار API). غير المستحق → `WARNING_LEVEL_NOT_REACHED` بالقيمة والحد؛
النوع الموقوف → `WARNING_TYPE_DISABLED`. الإصدار **لا يغير أي بيانات خام** (اختبار).
**لا PDF** (مرحلة 12).

## Void workflow

`POST /warnings/{id}/void/` للمدير حصرًا بسبب إلزامي؛ لا حذف نهائي. الإلغاء مرتين →
`WARNING_ALREADY_VOIDED`. **سياسة موثقة:** الملغى لا يحجز المستوى فيمكن إعادة
الإصدار (اختبار يثبت صفًا ثانيًا مستقلًا مع بقاء الملغى).

## Excuse-after-warning

السيناريو الحاسم مثبت خلفيًا وE2E: إصدار عند 5 أيام ثم اعتماد عذر ليومين →
‏`metric_value_at_issue = 5` ثابت، `current_metric_value = 3`، `metric_drifted = true`،
والإنذار يبقى `ISSUED`. `get_warning_current_metric` لا يلمس Snapshot.

## Dashboard / Student profile

لوحة `/warnings`: بطاقتان (طلاب بلغوا حد الغياب / حد التأخر) + قائمة بالقيمة الحالية
والمستوى المبلوغ والمستويات الصادرة وأزرار إصدار **للمستحق فقط**، وفلاتر (نوع/صف/
حالة: مستحق‑صدر‑الكل) وترقيم — وفرق صريح بين `DUE` و`ISSUED`. تبويب «الإنذارات» في
ملف الطالب: ملخص + قائمة + تفاصيل (عند الإصدار مقابل الحالي + تنبيه الانحراف +
الصف/الفصل من Snapshot) + إلغاء للمدير. تبويب «الإنذارات» في إعدادات المدرسة
(تفعيل + حدود ×3 لكل نوع + تحقق تصاعدي فوري، والمدير وحده يعدّل).

## Permissions / Tenant isolation

مصفوفة PERMISSIONS.md محدّثة. اختبارات API: المعلم والمرشد ممنوعان من الاستحقاق
والإصدار، والوكيل يقرأ القواعد ولا يعدلها ويصدر ولا يلغي، والمرشد يقرأ إنذارات
الطالب. العزل: إنذار/طالب مدرسة أخرى → 404 في كل المسارات، ولوحة المدرسة الأخرى
فارغة، ومدير A/معلم B يعمل في A ويرفض في B (اختبار + E2E).

## Audit / Purge

‏`WARNING_RULES_UPDATED` (العتبات فقط)، `STUDENT_WARNING_ISSUED`،
`STUDENT_WARNING_VOIDED` — معرفات وأرقام بلا اسم/هوية، **ولا حدث لحساب الاستحقاق**.
‏Purge: «إنذارات الطالب» في `PURGE_STEPS` و`student` بـPROTECT؛ `WarningRule` تبقى
(بيانات مدرسة).

## Phase 12 readiness

من `warning_id` وحده تنتج م12 المستند كما صدر: الحكم + بيانات الطالب + كل المقاييس
وقت الإصدار، بلا إعادة حساب وبلا اعتماد على القيد الحالي.

## Backend tests

**432/432 PASS** (‏402 regression مراحل 1→10 + **30 جديدة**: 21 نطاق + 9 API).
التغطية: القواعد والافتراضات والترتيب والعزل؛ عتبات الغياب؛ استثناء بعذر/جزئي/غير
محسوم/مختلط؛ التأخر الصباحي وفصله عن تأخر الحصص وتصحيح الوصول؛ الإصدار وSnapshot
وعدم مساس البيانات الخام؛ التكرار والنقر المزدوج؛ غير المستحق والموقوف؛ المستوى
الثاني المستقل؛ تغيير القواعد؛ العذر بعد الإنذار؛ النقل وتغيير الاسم؛ الإلغاء
وإعادة الإصدار؛ اللوحة؛ عدّاد الاستعلامات؛ الأدوار وmass assignment والعزل
وmulti-school. ‏ruff نظيف على المستودع، `check` نظيف، `check --deploy` exit 0.

## Frontend tests

**112/112 Vitest PASS** (‏99 regression + **11 جديدة** للإنذارات + **2** لتاريخ اليوم
المحلي): عرض حدود الغياب/التأخر،
منع الحفظ عند ترتيب خاطئ، الحفظ الصحيح، القراءة فقط للوكيل، بطاقات اللوحة وصف
مستحق بمستوياته، الإصدار بحمولة نظيفة (بلا قيم موثوقة)، الفلترة والحالة الفارغة،
إخفاء الرابط عن المعلم، تبويب الملف بالانحراف، الإلغاء بسبب، إخفاء الإلغاء عن
الوكيل. ‏typecheck + lint + build نظيفة.

## E2E

**الحزمة الكاملة: 36/36 PASS** (‏6.3 دقيقة، `workers:1` ضد Docker حقيقي) —
‏32 اختبار انحدار للمراحل 1→10 + 4 للمرحلة 11.

`warnings.spec.ts` — **4/4 PASS**:
1. استيراد صف فريد → بذر 3 أيام غياب كامل → المستوى الأول مستحق → الوكيل يصدر من
   الواجهة → يظهر في ملف الطالب («صدر عند 3 أيام»).
2. يومان إضافيان → المستوى الثاني مستحق والأول صادر (ولا زر لإعادة إصداره) → إصدار
   الثاني.
3. عذر معتمد ليومين → التفاصيل تعرض «عند الإصدار: 5 / حاليًا: 3» مع تنبيه الانحراف
   والإنذار باقٍ.
4. ثلاث حالات تأخر صباحي → إنذار تأخر مستقل (ملف الطالب: إنذارات الغياب 0 / التأخر 1).
5. تغيير القواعد إلى 2/4/7 → إعادة التقييم بالحدود الجديدة والإنذارات الصادرة كما هي.
6. العزل: مديرة مدرسة أخرى لا ترى شيئًا، والإصدار لطالب مدرسة أخرى → 404.

**اكتشاف أثناء الكتابة:** كل يوم يملك سياق حصص مجمدًا خاصًا (م8) وقد يختلف عدده،
فالبذر يقرأ حصص كل يوم من `/analytics/daily/?date=` بدل افتراض حصص اليوم الحالي.
وأضيف مفتاح `date` اختياري لأمر البذر DEBUG-only (سيناريوهات متعددة الأيام).

### خللان حقيقيان كشفهما التشغيل الكامل (وأُصلحا)

1. **تاريخ اليوم في الواجهة كان بتوقيت UTC.** `toISOString().slice(0,10)` في
   ملف الطالب وبطاقة إنشاء العذر يعطي **تاريخ الأمس** بين منتصف الليل والثالثة
   فجرًا بتوقيت +03، فتُطلب بيانات يوم خاطئ (ظهر في `excuses.spec.ts` بعد تجاوز
   منتصف الليل: بطاقة «غياب كامل بعذر» = 0 بينما الـAPI للتاريخ الصحيح = 1).
   الإصلاح: مساعد مشترك `utils/dates.ts:localIsoDate` يبني التاريخ من مكونات
   التقويم المحلية، ويستخدم في الفترة الافتراضية والفترات الجاهزة (اليوم/الأسبوع/
   الشهر) وفي مدى تواريخ العذر، مع اختبارَي وحدة على طرفَي اليوم. (الصفحات الأخرى
   كانت تبني التاريخ محليًا أصلًا ولم تتأثر.)
2. **`device-roster-sync.spec.ts` كان يتجاوز مهلة 60 ثانية.** السبب عمل حقيقي:
   مزامنة السجل تُنشئ أمرًا لكل طالب (‏492 أمرًا) يُنفَّذ ويُبلَّغ فرديًا عبر
   الجسر، فيستغرق ~2.2 دقيقة. رُفعت مهلة هذا الاختبار وحده إلى 240 ثانية بتعليق
   يوضح السبب، وأُضيف تنظيف: الاختبار يعطّل أجهزة المحاكاة التي خلفتها التشغيلات
   السابقة ويعطّل جهازه في النهاية (الأجهزة لا تُحذف لأنها تحمل تاريخًا) — نشط
   الآن 7 أجهزة بدل 34، فزمن كل نبضة جسر يبقى ثابتًا بدل أن ينمو تشغيلًا بعد آخر.

## Performance / Query counts

`benchmark_warnings` (‏20% من الطلاب بلغوا الحد):

| طلاب | مقياس الغياب | مقياس التأخر | اللوحة المجمعة | طالب واحد |
|---|---|---|---|---|
| 500 | 17ms | 15ms | — | 22ms |
| 1000 | 27ms | 26ms | — | 22ms |
| 3000 | 113ms | 111ms | — | 26ms |
| 5000 | 151ms | 145ms | **296ms** | 20ms |

**تحسين أثناء القياس:** اللوحة كانت 1.4s ثابتة لأنها تحمّل قيود وأسماء كل طلاب
المدرسة؛ عُكس الترتيب (المقاييس المجمعة أولًا ثم بيانات المرشحين فقط) → 296ms عند
5000 طالب. اختبار `django_assert_max_num_queries(12)` يثبت ثبات عدد الاستعلامات.

## Fresh migration / Docker

قاعدة جديدة `xmansx_fresh11` → migrate كامل PASS ثم حذفت؛ `makemigrations --check`
بلا تغييرات؛ ‏OpenAPI ‏`spectacular --validate` exit 0. الخدمات الست سليمة (لا خدمة
جديدة، ولا Celery في مسار الإنذارات).

## Security review

لا قيم موثوقة من العميل (اختبار حقن يثبت التجاهل)؛ إعادة حساب خادمية عند الإصدار؛
قيد قاعدة بيانات ضد الإصدار المزدوج؛ لا تزوير للتاريخ (الحدود/الأعذار/النقل لا تمس
الصادر)؛ لا PII في Audit ولا في قوائم الإنذارات (لا رقم هوية كامل)؛ عزل مستأجرين
مختبر على كل مسار.

## Known limitations

- اليوم المختلط لا يحتسب (سياسة MVP) — تغييرها نقطة واحدة موثقة.
- إنذارات التأخر تعتمد عدد المرات فقط؛ مجموع الدقائق محفوظ في Snapshot ولم يستخدم
  كمقياس عتبة (نوع لاحق ممكن بلا تغيير بنية).
- النطاق سنوي: لا تصفير فصلي (قرار موثق بالأدلة).
- `MORNING_LATE_OCCURRENCES` مقيد بنافذة تواريخ العام لأن `SchoolArrival` بلا FK
  للعام — إضافة FK لاحقًا تجعله أدق (دين موثق).

## Technical debt

- ‏FK عام دراسي على `SchoolArrival` (يبسط المرساة).
- أنواع القواعد الإضافية (`PERIOD_LATE_OCCURRENCES`, `UNEXCUSED_ABSENT_PERIODS`)
  جاهزة بنيويًا وغير منفذة.
- تحذيرات OpenAPI القديمة (‏non-blocking، خطة OPENAPI.md).

## Files changed

Backend: وحدة `student_warnings/` كاملة (models/migration/services×2/selectors/api×2/
urls/admin/purge_integration/benchmark) + `audit/models.py` (3 أحداث) +
`config/{settings,urls}` + `excuses/selectors.py` (استخراج الفلتر المرجعي + kwarg
اختياري) + `attendance/.../seed_attendance_sessions.py` (مفتاح date) +
`accounts/.../seed_dev.py` (اتساق سنة التطوير) + اختبارات (‏test_warnings.py،
‏test_warnings_api.py، ‏tests/excuse_env.py المشترك).
Frontend: ‏`features/warnings/` (api + WarningsDashboardPage + StudentWarningsTab +
warnings.test.tsx) + `settings/tabs/WarningRulesTab.tsx` + SettingsPage +
StudentAttendanceProfilePage + routes + AppShell.
E2E: ‏`warnings.spec.ts` + `generate_e2e_fixtures.py` (noor-8) +
`device-roster-sync.spec.ts` (مهلة صريحة + تنظيف الأجهزة).
إصلاح التاريخ المحلي: `utils/dates.ts` (+ اختباره) و`StudentAttendanceProfilePage`
و`ExcuseCreateCard`.
Docs: ‏WARNING_RULES.md وSTUDENT_WARNINGS.md جديدتان + تحديث ARCHITECTURE/ERD/
PERMISSIONS/SECURITY/DATA_PURGE/STUDENT_ATTENDANCE_PROFILE/ABSENCE_EXCUSES.

## Commit

`feat: add configurable warning rules and student warnings` — بلا Push.

**تنبيه حوكمة (يستوجب قرارك):** أثناء تنفيذ هذه المرحلة كانت **جلسة موازية تعمل على
نفس الشجرة**، وفي الساعة 01:14 التقطت شجرة العمل كاملة والتزمتها ضمن commit المرحلة
العاشرة `68494f6 feat: add absence excuses and excused absence classification` —
فدخل كامل كود المرحلة 11 (‏`backend/student_warnings/` و`features/warnings/`
و`test_warnings*.py` و`PHASE_11_REPORT.md`) داخل commit يحمل عنوان المرحلة 10.
لم أُعِد كتابة ذلك التاريخ: الـcommit ليس ملكي، والجلسة الأخرى نشطة، وإعادة الكتابة
عملية هدمية تحتاج قرارك. الـcommit الحامل للرسالة المطلوبة يضم بقية عمل المرحلة
(إصلاحا التاريخ المحلي وE2E والتقرير). إن أردت فصلًا نظيفًا للمرحلتين فالخيار
المتاح هو إعادة كتابة `68494f6` إلى commitين — بأمر صريح منك وبعد إيقاف الجلسة
الموازية.

## Ready for Phase 12?

نعم — Snapshot الإنذار يكفي وحده لإنتاج مستند «الإنذار الأول/الثاني/الثالث» كما صدر،
والحقول والصلاحيات والتدقيق والحذف النهائي كلها جاهزة. **متوقف بانتظار الاعتماد.**
