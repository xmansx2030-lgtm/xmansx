# PHASE 13 — إحالات الطلاب إلى المرشد الطلابي

**التاريخ:** 2026-08-20 · **الفرع:** `feature/phase-13-student-referrals`
**الشجرة:** `xmansx-phase-13/` (worktree مستقل) · **الأساس:** `a552f9c` = PHASE 11 COMPLETE
**الحالة:** ✅ مكتملة — كل معايير القبول محققة ومثبتة بتشغيل فعلي.

---

## 1. Status

| البوابة | النتيجة |
|---|---|
| Backend pytest (كل الحزمة) | ✅ **485/485** (منها **53** اختبار م13) |
| ruff | ✅ نظيف |
| Django check | ✅ 0 issues |
| Fresh migrate (قاعدة PostgreSQL جديدة) | ✅ |
| `makemigrations --check` | ✅ لا تغييرات معلقة |
| Frontend typecheck + eslint | ✅ |
| Vitest | ✅ **123/123** (17 ملفًا؛ منها 11 اختبار إحالات) |
| Frontend build | ✅ |
| Playwright — م13 | ✅ **6/6** (السيناريوهات الخمسة الإلزامية + العزل) |
| Playwright — الحزمة الكاملة | ✅ **42/42** (كل المواصفات القديمة + م13) |
| Docker (حزمة معزولة كاملة) | ✅ Healthy |
| Benchmarks | ✅ عدد استعلامات ثابت مع الحجم (§13) |

## 2. عزل التطوير المتوازي

المرحلة 12 تعمل بالتوازي في الشجرة الرئيسية. العزل المطبق:

| الطبقة | الشجرة الرئيسية (م12) | شجرة م13 |
|---|---|---|
| Worktree | `xmansx/` | `xmansx-phase-13/` |
| Branch | `feature/phase-10-absence-excuses` | `feature/phase-13-student-referrals` |
| Docker project | `xmansx` | `xmansx13` |
| المنافذ | 8000 / 5173 / 5433 / 6379 | 8100 / **5273** / 5533 / 6479 |
| قاعدة البيانات | حجم `xmansx_pgdata` | حجم مستقل (بدأت فارغة ⇒ **Fresh migrate مثبت**) |

**لم يُلمس worktree الوكيل الآخر ولا حزمته ولا قاعدته.** ثلاثة تمكينات صغيرة جعلت
هذا ممكنًا (كلها بقيم افتراضية محفوظة فلا تغير سلوك الشجرة الرئيسية):

1. `E2E_COMPOSE_PROJECT` — يوجه أوامر البذر في E2E إلى حزمة الشجرة الحالية.
   **بدونها كانت مواصفات E2E القائمة تبذر داخل قاعدة الوكيل الآخر** (اكتُشف عمليًا:
   7 إخفاقات في أول تشغيل كامل سببها ضرب الحزمة الخطأ).
2. `E2E_BASE_URL` — يوجه Playwright إلى واجهة هذه الشجرة.
3. `DEV_EXTRA_ORIGINS` — يضيف أصل التطوير الجديد إلى CSRF/CORS (`local.py` فقط،
   والإنتاج لا يقرأه).

**تعارض مُتفادى:** الوكيل الآخر حجز `noor-9.xlsx` لملف مستندات م12، فنُقل ملف
إحالاتي إلى `noor-10.xlsx` وبادئة هوياته إلى `4` (بادئة 7 مستخدمة مرتين لديه —
تُحسم عند الدمج).

## 3. لا اعتماد على المرحلة 12

`StudentAction` و`GeneratedDocument` **غير موجودين في هذا الأساس** (تحقق مباشر).
لم يُنشأ أي Placeholder Model. TODO التكامل موثق في `docs/STUDENT_REFERRALS.md`:
بعد دمج م12 يمكن أن يُنشئ إنشاءُ الإحالة `StudentAction(REFERRED_TO_COUNSELOR)`.

كذلك **لا بنية إشعارات في المشروع** (تحقق بالبحث) — فلا إشعارات داخلية في هذه
المرحلة، ولا SMS/WhatsApp/Email بأي حال.

## 4. النموذج والفئات

ثلاثة نماذج في تطبيق `referrals/` وهجرة واحدة:
`StudentReferral` + `StudentReferralContribution` + `StudentReferralEvent`.

- `source_type` snapshot لدور المُحيل لحظة الإحالة (أعلى أدواره صلاحية).
- خمس فئات و22 سببًا، والخريطة `REASONS_BY_CATEGORY` مصدر حقيقة واحد يخدم التحقق
  والواجهة معًا (`GET /referrals/options/`).
- **المعلم:** دراسي/سلوك صفي/اجتماعي/عام. **الوكيل والمدير:** كل الفئات بما فيها
  المواظبة. الخادم يفرض ذلك ولا يعتمد على إخفاء الواجهة.
- سبب «أخرى» يلزمه وصف مكتوب.

## 5. سياسة «لا تشخيص»

لا حقل تشخيص طبي/نفسي في أي نموذج، والواجهة تطلب صراحة **ملاحظة واقعية قابلة
للملاحظة**. اختبار `test_no_diagnosis_fields_in_schema` يفشل إن تسلل حقل بهذا
المعنى مستقبلًا.

## 6. الحالات

`NEW → ACKNOWLEDGED → CLOSED` + `CANCELLED` للإلغاء قبل/بعد الاستلام. أربع حالات
**كلها مستعملة** — لم تُضف حالات م14 الوسيطة لأنها بلا انتقالات حقيقية بعد
(«لا workflow وهمي»). قيد DB يضمن اتساق `closed_at` مع الحالة. لا حذف نهائي.

## 7. التعيين والاستلام

- التعيين للمدير/الوكيل فقط؛ **المعلم ينشئ ولا يعيّن**.
- المرشد الصالح: عضوية `ACTIVE` + دور `COUNSELOR` + نفس المدرسة. المرشد الأجنبي
  والموقوف وغير المرشد كلها مرفوضة (`INVALID_COUNSELOR_ASSIGNMENT`).
- الإنشاء بلا مرشد مسموح (سياسة MVP)؛ `NO_ACTIVE_COUNSELOR_AVAILABLE` عند التعيين
  في مدرسة بلا مرشد — لا فشل صامت.
- إعادة التعيين تُسجَّل حدثًا يحمل المرشد السابق.
- الاستلام للمرشد المعيَّن وحده؛ الحالة غير المعينة تُطالَب داخل نفس القفل فلا
  يستلمها مرشدان.

## 8. كشف التكرار وسير الملاحظات

الوحدة: **(الطالب، الفئة) + حالة مفتوحة**. الإنشاء يرد `409` مع
`existing_referral_id` و`recommended_action=ADD_CONTRIBUTION`، والواجهة تعرض
الخيار الافتراضي «إضافة ملاحظة». فئة مختلفة أو حالة قديمة مغلقة → إحالة جديدة.
تجاوز التكرار للمدير/الوكيل فقط.

**كيف يضيف المعلم ملاحظة لحالة لا يراها بعد؟** عبر `POST /referrals/contribute/`
بـ(الطالب، الفئة) — **الخادم يحل الحالة بنفسه ولا يقبل معرف إحالة من العميل**.
المحاولة الأولى سمحت بالمعرف الحر فكشفت المراجعة أنها بوابة قراءة لكل حالة مفتوحة
في المدرسة (§15 البند 1). القراءة تُفتح بعد المساهمة الحقيقية فقط.

## 9. اللقطة مقابل الحالي

اللقطة تُلتقط مرة واحدة وتبقى: الاسم والصف والفصل دائمًا، ومؤشرات المواظبة
والإنذارات لفئة المواظبة فقط (بلا هوية ولا جوال ولي أمر). التفاصيل تعرض عمودين
صراحة: **وقت الإحالة** و**حاليًا**. مثبت باختبار نطاق وE2E: 2 يوم بدون عذر وقت
الإحالة → 1 حاليًا بعد اعتماد عذر، واللقطة لم تتحرك.

## 10. الصلاحيات والعزل

| الدور | عرض | إنشاء | تعيين | استلام | إغلاق | ملاحظة |
|---|---|---|---|---|---|---|
| مدير | الكل | ✅ | ✅ | ❌ | ✅ | ✅ |
| وكيل | الكل | ✅ | ✅ | ❌ | ✅ | ✅ |
| مرشد | المعيّن له + غير المعيّنة | ❌ | ❌ | ✅ | ✅ | ✅ |
| معلم | ما أنشأه أو ساهم فيه | ✅ فئاته | ❌ | ❌ | ❌ | ✅ |

العزل: إحالة/طالب/مرشد من مدرسة أخرى → **404** (أو 400 للتعيين)، ونطاق الرؤية
يُطبَّق قبل أي فلتر من العميل. مستخدم بدورين في مدرستين تتبع صلاحياته المدرسة
النشطة (مثبت في اختبار API وE2E).

## 11. Audit والحذف النهائي

ستة أحداث `REFERRAL_*` بمعرفات وأعداد فقط — **لا نسخة من نص الملاحظة** (الإحالة
مصدر المحتوى). الخط الزمني (`StudentReferralEvent`) منفصل عن Audit عمدًا: الأول
للمستخدم والثاني للأمان.

Purge: ثلاث خطوات بترتيب التبعية (الأحداث ← الملاحظات ← الإحالات)، و`student`
بـ`PROTECT` فالنسيان يفشل صاخبًا. مغادرة المرشد لا تحذف الحالة (`SET_NULL`).

## 12. الواجهة

- **المعلم:** زر «تحويل للمرشد» على كل صف في قائمة الفصل داخل شاشة التحضير
  (حيث يلاحظ الطالب فعلًا) + صفحة «إحالاتي» + رابط تنقل — وهو **أول رابط تنقل
  للمعلم في المنصة** (كانت كل الروابط للإدارة).
- **الإدارة:** تبويب «الإحالات» في ملف الطالب مع زر التحويل.
- **المرشد:** صفحة «الإحالات» بمؤشرات (جديدة/تم استلامها/غير معينة) وفلاتر
  (الحالة، الفئة، المصدر، التعيين) وبطاقة تفاصيل تجمع اللقطة والحالي والملاحظات
  والخط الزمني وإجراءات دورة الحياة.
- نموذج الإنشاء واحد مشترك بين المسارات الثلاثة، وقوائمه تأتي من الخادم حسب الدور.

## 13. الأداء

`python manage.py benchmark_referrals`:

| العملية | الزمن | الاستعلامات |
|---|---|---|
| قائمة 25 من أصل 100 / 1000 / 5000 | 21 / 29 / 44ms | **4 · 4 · 4** |
| صندوق وارد المرشد (نفس الأحجام) | 18 / 24 / 45ms | **4 · 4 · 4** |
| مؤشرات KPIs | 3–6ms | **1** |
| كشف التكرار (تاريخ 1 / 10 / 50) | 6.0 / 6.3 / 6.8ms | **1 · 1 · 1** |
| تفاصيل إحالة | 17ms | **6** |

**لا N+1:** عدد الاستعلامات ثابت تمامًا مع الحجم في القائمة وصندوق الوارد وكشف
التكرار والتفاصيل.

## 14. الاختبارات

| النوع | العدد | التغطية |
|---|---|---|
| Backend — النطاق (`test_referrals.py`) | 34 | الإنشاء، قيود الفئات، اللقطة، التكرار، التعيين، الاستلام، الإغلاق/الإلغاء، النطاق حسب الدور، KPIs، Purge |
| Backend — API (`test_referrals_api.py`) | 19 | سير العمل الكامل، options، خصوصية المعلم، سياسة المرشد، العزل/IDOR، متعدد المدارس، Mass assignment |
| Vitest (`referrals.test.tsx`) | 11 | نموذج المعلم من قائمة الفصل، تقييد الفئات، إلزام الوصف، تحذير التكرار وإضافة ملاحظة، صندوق الوارد، الفلاتر، الاستلام، التعيين، الإغلاق، اللقطة مقابل الحالي، منع المعلم |
| Playwright (`referrals.spec.ts`) | 6 | السيناريوهات الخمسة الإلزامية + عزل إحالة أجنبية |

**السيناريوهات الإلزامية (154–158) — كلها خضراء:**

1. معلم يحيل «النوم داخل الحصة» → تظهر في «إحالاتي» → المرشدة تراها جديدة وتستلمها.
2. وكيل يحيل «غياب متكرر» (لقطة مواظبة) → يعيّن المرشدة.
3. معلم ثانٍ يصطدم بالتكرار → يضيف ملاحظة → حالة واحدة بملاحظتين.
4. اللقطة ثابتة بينما المؤشر الحالي ينخفض بعد اعتماد عذر.
5. مستخدم معلم في مدرسة ومرشد في أخرى — لا تسرب صلاحيات ولا بيانات.

## 15. مراجعة تدقيقية مستقلة — عيوب اكتُشفت وصُححت

بعد اجتياز البوابات شُغّلت مراجعة تدقيقية مستقلة على كامل كود المرحلة. **كل ما
يلي صُحح وأضيفت له اختبارات انحدار:**

| # | العيب | الأثر | الإصلاح |
|---|---|---|---|
| 1 | **ثغرة خصوصية:** إرسال ملاحظة بمعرف حر كان مسموحًا لأي حالة مفتوحة في فئة يملكها الدور | المعلم يرسل ملاحظة تافهة للمعرفات 1..N فيقرأ **كل حالة مفتوحة في المدرسة** (الوصف، اسم الطالب، ملاحظات الزملاء) لأن المساهمة تمنح الرؤية؛ وفرق 201/404 تعداد صالح | نقطة نهاية `POST /referrals/contribute/` تحل الحالة خادميًا من (الطالب، الفئة)؛ المسار بالمعرف صار يتطلب رؤية فعلية |
| 2 | نفس المسار كان يرد 403 للمرشد على حالة زميله بينما 404 لغير الموجود | تمييز «موجودة» عن «غير موجودة» — تسريب وجود | زال بزوال المسار الاستثنائي |
| 3 | تجميع الأدوار: `visible_referrals` كانت تعيد عند أول دور مطابق | مرشد يحمل دور معلم يفقد رؤية إحالته بعد تعيينها لزميل — تظهر في «إحالاتي» وفتحها 404 | النطاقات تُجمع بـOR، والمشاركة تمنح الرؤية لأي دور |
| 4 | نافذة مؤشرات اللقطة بلا سقف | عام نشط أقدم من 366 يومًا ⇒ `ValueError` غير معالج ⇒ **500** على إنشاء أي إحالة مواظبة وعلى قراءة تفاصيلها (بل وبعد نجاح الاستلام/الإغلاق) | قص النافذة إلى حد ملف الطالب |
| 5 | فحص التكرار خارج المعاملة بلا قفل | طلبان متزامنان لنفس (الطالب، الفئة) ينشئان حالتين مفتوحتين | الفحص والإنشاء داخل معاملة واحدة مع `select_for_update` على صف الطالب |
| 6 | إلغاء المنشئ بلا قيد حالة | المعلم يسحب حالة استلمها المرشد وبدأ العمل عليها | الإلغاء للمنشئ قبل الاستلام فقط؛ بعده قرار إداري |
| 7 | كائن `ApiError` وحيد على مستوى الوحدة يُرفع في كل طلب | تراكم `__traceback__` عبر الطلبات وتثبيت إطارات في الذاكرة | مصنع يعيد استثناءً جديدًا |
| 8 | رفض راية `allow_duplicate` من المعلم قبل التحقق من وجود تكرار | رفض بسبب خاطئ حتى حين لا يوجد تكرار أصلًا | الراية تُتجاهل لمن لا يملكها، والرد يعكس السبب الحقيقي |
| 9 | **واجهة:** نموذج الإحالة بلا `key` | اختيار طالب ثانٍ يعيد استخدام النموذج نفسه ⇒ **الملاحظة تذهب لملف الطالب الأول** ووصفه ينتقل صامتًا | `key={referralTarget.id}` |
| 10 | لوحة التكرار طريق مسدود عند فشل الإضافة | لو أُغلقت الحالة لحظتها بقي المستخدم عالقًا بينما إحالة جديدة صارت مقبولة | زر «العودة إلى النموذج» |
| 11 | زر «إلغاء الإحالة» ظاهر للجميع | نقرة تُرد بـ403 | `can_cancel` يحسبه الخادم |
| 12 | قيمة `status` غير معروفة في الفلتر تُتجاهل | العميل يظن القائمة مفلترة وهي كل الحالات | 400 برسالة عربية |

المراجعة أكدت سلامة: منع Mass Assignment، سباقات الاستلام/التعيين/الإغلاق
(`select_for_update` داخل المعاملة)، ثبات اللقطة بعد الإنشاء، العزل بين المدارس،
ثبات عدد الاستعلامات (لا N+1)، وخلو النماذج والواجهة من أي حقل تشخيص.

## 16. الملفات

**جديد:** `backend/referrals/` (models, selectors, services/{referrals,snapshots},
api/{serializers,views}, urls, admin, apps, purge_integration, migrations/0001,
management/commands/benchmark_referrals) · `backend/tests/test_referrals*.py` ·
`frontend/src/features/referrals/` (api, ReferralCreateCard, ReferralDetailCard,
ReferralsPage, MyReferralsPage, StudentReferralsTab, referrals.test.tsx) ·
`frontend/e2e/{referrals.spec.ts,compose.ts}` · `docs/STUDENT_REFERRALS.md` ·
`PHASE_13_REPORT.md`

**معدّل (بأقل قدر ممكن):** `config/{settings/base,settings/local,urls}` ·
`audit/models` · `accounts/.../seed_dev` (مرشدة في مدرسة الاختبار) ·
`frontend/src/{app/AppShell,routes/index}` (روابط ومسارات) ·
`AttendanceSessionPage` (زر التحويل) · `StudentAttendanceProfilePage` (تبويب) ·
`playwright.config` + `e2e/global-setup` + 3 مواصفات E2E (توجيه compose) ·
`scripts/generate_e2e_fixtures.py` · `.gitignore`

## 17. حدود معروفة ودين تقني

1. **لا إشعارات** — لا بنية إشعارات في المشروع؛ المرشد يرى الجديد من صندوق الوارد.
2. **`priority` موجود ولا واجهة له** — النموذج يدعم `NORMAL/HIGH` ويُخزَّن، لكن لم
   تُبنَ عناصر تحكم في الواجهة لعدم وجود حاجة معلنة (بند 15 يسمح بذلك صراحة).
3. **الحالات الوسيطة لم تُنفَّذ** — تُضاف في م14 مع انتقالاتها.
4. **الملاحظات غير قابلة للتعديل** — قرار MVP (لا تعديل صامت)؛ التعديل يحتاج سياسة
   وAudit إن طُلب لاحقًا.
5. **بذر مرشدة جديدة في `seed_dev`** (`0550000005`) — كانت مدرسة الاختبار بلا مرشد
   إطلاقًا فتعذر إثبات «معلم يحيل ← مرشد يستلم» بمستخدمين مختلفين.
6. **تكامل م12 مؤجل** بالكامل كما نص التكليف.

## 18. جاهزية م14 وترتيب الدمج

الأساس جاهز: الحالة وخطها الزمني ونطاق الرؤية والملاحظات — تبني عليها م14
`CounselorAction` و`FollowUpPlan` و`TeacherFollowUpRequest`. **لم يُنفَّذ من م14 شيء.**

ترتيب الدمج الموصى به (بند 145): دمج م12 ← إعادة أساس م13 عليه ← حل تعارضات
`AppShell`/`routes`/`generate_e2e_fixtures`/`audit` ← تكامل `StudentAction`
الاختياري ← انحدار كامل.

**نقاط تعارض متوقعة ومعروفة مسبقًا:** `audit/models.py` (كتلتان متجاورتان)،
`config/settings/base.py` و`urls.py` (سطر لكل تطبيق)، `AppShell.tsx` و
`routes/index.tsx` (روابط ومسارات)، `generate_e2e_fixtures.py` (ملفان بأرقام
مختلفة عمدًا)، `StudentAttendanceProfilePage.tsx` (تبويب لكل مرحلة).
