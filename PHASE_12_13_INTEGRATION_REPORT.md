# PHASE 12 + 13 — تقرير الدمج والتحقق الكامل

**التاريخ:** 2026-08-20

## Status

`INTEGRATION COMPLETE` — كل بنود بوابة القبول (البند 95) تحققت بتشغيل فعلي موثق
أدناه على **شجرة مدمجة وقاعدة بيانات جديدة تمامًا**. لا بند NOT RUN.

## Integration base

| العنصر | القيمة |
|---|---|
| قاعدة المرحلة 12 | `a385573` (شجرة `feature/phase-10-absence-excuses`) |
| فرع المرحلة 13 | `feature/phase-13-student-referrals` @ `e9e3268` (شجرة `xmansx-phase-13`) |
| الأصل المشترك | `a552f9c` (المرحلة 11) |
| نقطة الحماية | `rescue/pre-phase-12-13-integration` → `a385573` (لم تُحذف) |
| فرع الدمج | `integration/phase-12-13` |

**تجميد الكتّاب:** الشجرتان نظيفتان قبل البدء (`git status` فارغ في كليهما)، وعمل
المرحلة 13 ملتزم بالكامل في `e9e3268`. لم أكتب في شجرة المرحلة 13 إطلاقًا — كل
العمل داخل فرع الدمج في الشجرة الرئيسية. حزمتا Docker منفصلتان (`xmansx` و
`xmansx13`) فلم يلمس أي تشغيل قاعدة الآخر.
**لم يُعد كتابة أي تاريخ**: `68494f6` و`cfce2b3` كما هما (لا rebase ولا reset ولا
filter-branch).

## Merge strategy

`git merge --no-ff` للمرحلة 13 فوق المرحلة 12 داخل فرع الدمج — لا rebase: فرع
المرحلة 13 مرجع حي لجلسة أخرى، وإعادة كتابته لتجميل التاريخ مخاطرة بلا مقابل
(البند 8: سلامة الشجرة أولًا). ولا cherry-pick يدوي: Git احتفظ بتاريخ المرحلتين.

## Conflicts found / resolved

أربعة تعارضات، كلها «إضافة مقابل إضافة» وحُلّت **بضم الطرفين** لا باختيار أحدهما:

| الملف | التعارض | الحل |
|---|---|---|
| `config/settings/base.py` | `INSTALLED_APPS` | الثلاثة معًا: `student_actions`, `documents`, `referrals` |
| `config/urls.py` | تسجيل المسارات | ثلاثة `include` بلا تصادم بادئات |
| `StudentAttendanceProfilePage.tsx` | التبويبات | الإجراءات + المستندات + الإحالات معًا |
| `scripts/generate_e2e_fixtures.py` | مولّد الـfixtures | كتلتا م12 وم13 ومفاتيح meta للاثنين |

الدمج التلقائي نجح في `audit/models.py` و`.gitignore` و`global-setup.ts` و
`playwright.config.ts` — وتحققت يدويًا أن كل طرف بقي: أحداث التدقيق للمرحلتين،
وتسخين Vite (م12) مع عزل الحزم (م13) معًا.

## Noor fixture resolution

`noor-9` (م12) و`noor-10` (م13) بقيا كما هما بأسماء مالكيهما. وأضيف:
`noor-11` لسيناريوهات التكامل، و`noor-12` لمزامنة أجهزة الطلاب (انظر أدناه).

## E2E prefix resolution — تصادم حقيقي اكتُشف وأُصلح

**البادئة `7` كانت مستخدمة مرتين**: التحليلات (م8) والمستندات (م12) تولّدان نفس
أرقام الهوية. ورقم الهوية هو **مفتاح تطابق الطالب في الاستيراد**، فملف م12 كان
ينقل طالبَي التحليلات إلى صفه بدل إنشاء طلاب جدد. عيب أدخلته المرحلة 12 ولم يظهر
لأن اختباراتها تستخدم المعرفات المعادة من البذر.

الإصلاح: سجل بادئات صريح في رأس المولّد + نقل المستندات إلى البادئة `3`، و
**حارس تنفيذي** (`_assert_unique_ids`) يفشل توليد الـfixtures عند أي تصادم بين
عائلتين (‏noor-1/2 عائلة واحدة وnoor-3/3b عائلة واحدة — تشاركهما مقصود). تحققت من
الحارس بإعادة التصادم عمدًا: خرج بالرمز 1.

| النطاق | المرحلة |
|---|---|
| `1{tag}NNN` | الأساس (م4) — 001..0xx |
| `2{tag}NNN` | دورة الحياة (م4.1) |
| `1{tag}1NN` | تكامل م12+م13 |
| `1{tag}2NN` | مزامنة أجهزة الطلاب (م8.6) |
| `1{tag}3NN` | المستندات (م12) |
| `1{tag}4NN` | الإحالات (م13) |
| `1{tag}5NN` | الأعذار (م10) |
| `1{tag}6NN` | الإنذارات (م11) |
| `1{tag}7NN` | التحليلات (م8) |
| `1{tag}8NN` | متابعة التحضير (م7) |
| `1{tag}9NN` | الحضور وQR (م6) |

## Docker / E2E isolation

تحسين المرحلة 13 محفوظ بالكامل: `E2E_COMPOSE_PROJECT` و`E2E_BASE_URL` و
`E2E_BACKEND_URL` و`E2E_PYTHON` و`DEV_EXTRA_ORIGINS`. والافتراضات بلا متغيرات
بيئة تبقى سلوك الشجرة الرئيسية كما هو. **ووحّدت ما تخلّف عنه**: `documents.spec`
كان ينادي `docker compose` مباشرة و`auth.spec` كان يثبّت `localhost:5173` — كلاهما
صار يمر عبر `e2e/compose.ts`، فلا يبذر تشغيل شجرة داخل قاعدة شجرة أخرى.

## Installed apps / URL integration / Migration graph

التطبيقات الثلاثة مسجلة معًا، والمسارات بلا تصادم:
`/api/v1/student-actions/…` و`/api/v1/documents/…` و`/api/v1/referrals/…`.
`showmigrations` بلا أوراق متعارضة ولا حاجة لهجرة دمج: كل تطبيق شجرته مستقلة
(`documents 0001,0002` — `referrals 0001` — `student_actions 0001,0002`).
الهجرة الجديدة الوحيدة `student_actions.0002` لإضافة نوع الإجراء الجديد.

## Purge integration

الترتيب الفعلي بعد الدمج (مطبوع من التطبيق الحي ومقفول باختبار):

```
أحداث الإحالات → ملاحظات الإحالات → إحالات الطالب →
المستندات المولدة → الإجراءات الطلابية → إنذارات الطالب →
تغطيات/أهداف/مرفقات/أعذار → أجهزة ووصول صباحي → حضور → القيود
```

الأبناء قبل الآباء، وكل ما يشير بـPROTECT يُحذف قبل هدفه.
`PURGE_STORAGE_COLLECTORS` يحتفظ بجامع ملفات PDF (‏`collect_generated_document_files`)
بعد إضافة الإحالات — اختبار يثبته صراحةً.

## Permission matrix

لم تتوسع صلاحيات م12 بدمج م13 (مثبت باختبارات API):

| | مدير | وكيل | مرشد | معلم |
|---|---|---|---|---|
| الإجراءات | كاملة | كاملة | قراءة فقط | ممنوع |
| المستندات | كاملة + إلغاء | إنشاء/قراءة/تنزيل | بيانات وصفية فقط (لا تنزيل ولا لقطة) | ممنوع |
| الإحالات | إنشاء/تعيين/قراءة | إنشاء/تعيين/قراءة | استلام/إغلاق/ملاحظات | إنشاء ضمن فئاته + إحالاته فقط |
| أثر «إحالة إلى المرشد» | يُسجَّل | يُسجَّل | — | **لا يُسجَّل** |

## Student profile integration

ملف الطالب يجمع في جلسة واحدة: الملخص وسجل الأيام وغياب الحصص وتأخر الحصص والحضور
الصباحي والأعذار والإنذارات والإجراءات والمستندات والإحالات (وسجل التعديلات
للمدير/الوكيل). اختبار Vitest يقفل القائمة كاملة فلا يبتلعها دمج لاحق، وE2E يفتح
التبويبات الخمسة الحساسة في جلسة واحدة.

## StudentAction ↔ Referral integration

فحصت الـEnum أولًا (البند 32): لا نوع مكافئ، فأضيف `REFERRED_TO_COUNSELOR`.
السياسة المعتمدة:

- إحالة **الوكيل/المدير** تسجل إجراءً واحدًا داخل **نفس معاملة** إنشاء الإحالة،
  ويحمل نفس `source_warning` إن وُجد.
- إحالة **المعلم لا تنتج إجراءً إداريًا** (البند 33): سجلها هو الإحالة نفسها.
- رفض الإحالة (تكرار مفتوح) يمنع الإجراء كذلك — اختبار يثبت عدم بقاء أثر يتيم.
- **مصدر الحقيقة `StudentReferral`** والإجراء أثر إداري (البند 35) — لا FK من
  الإجراء إلى الإحالة كي لا ينقلب ترتيب الحذف ولا تصبح الحالة مزدوجة المصدر.

## Warning → Document → Action → Referral

السلسلة كاملة بلا اعتماد دائري (اختبار خلفي + E2E): إنذار ثانٍ عند 5 أيام ← مستند
PDF ← إجراء «تسليم إنذار» ← إحالة مرتبطة بنفس الإنذار ← صندوق المرشدة ← استلام.
إجراء التسليم وإجراء الإحالة صفّان مستقلان لا يختلطان.

## Document immutability

بصمة المستند ومحتواه لم يتغيرا بعد: إنشاء الإحالة، تعيين المرشدة، الاستلام، ثم
اعتماد عذر لخفض المقياس الحالي — `checksum` قبل = بعد، واللقطة ما زالت 5 أيام
(اختبار خلفي + E2E ينزّلان الملف فعليًا ويقارنان البايتات).

## Referral snapshot immutability

`snapshot_at_referral` لم تتغير بإنشاء مستند أو إجراء بعدها، وبعد اعتماد عذر:
اللقطة 5 والقيمة الحالية 3 — مشتقة منفصلة كما هي.

## Security review / Tenant isolation / IDOR

- مديرة مدرسة أخرى: 404 على الإجراء والمستند وتنزيله والإحالة وملاحظاتها،
  والقوائم صفر (اختبار API + E2E).
- المرشدة المسندة على حالة: لا تنزيل مستند ولا قراءة لقطته.
- المعلم: يحيل ضمن فئاته فقط، ومحجوب عن المستندات والإجراءات الإدارية، ولا يضيف
  ملاحظة بمعرف إحالة حر (إصلاح م13 محفوظ).
- متعدد الأدوار عبر مدرستين: معلم في A ومرشدة في B — لا توارث صلاحيات في أي اتجاه.
- لا قيم لقطة موثوقة من العميل في أي من الوحدات الثلاث.

## Backend tests

**561/561 PASS** داخل الحاوية (`DJANGO_SETTINGS_MODULE=config.settings.test`)
و**0 تخطٍّ** — 495 من المرحلة 12 + 47 من المرحلة 13 + **13 تكامل جديدة** + 6
تغطية إضافية بعد الدمج (منها اختبار الاقتطاع).

**المجموعات المركّزة: 269/269 PASS** —
`student_actions`, `documents`(+API), `referrals`(+API), `integration`(+API),
`student_warnings`(+API), `excuses`(+API), student profile, attendance,
device roster, lifecycle/purge, permissions.

‏ruff نظيف · `manage.py check` نظيف · `check --deploy` exit 0 · `makemigrations --check`
بلا تغييرات.

## Frontend tests

**143/143 Vitest PASS** (‏138 بعد الدمج + **5 اختبارات تكامل جديدة**: بقاء تبويبات
كل الوحدات، تبويبات المرشد بلا سجل التعديلات، وتنقّل الأدوار الثلاثة).
‏typecheck + lint + build نظيفة.

## Playwright

**52/52 PASS** (‏8.5 دقيقة، `workers:1`، **بلا استخدام أي إعادة محاولة**) على
قاعدة بيانات أنشئت من الصفر — 47 اختبار انحدار للمراحل 1→13 + **5 اختبارات تكامل
جديدة** (`integration.spec.ts`):

1. إنذار ← مستند PDF من الواجهة ← إجراء «تسليم إنذار» ← الثلاثة في ملف الطالب.
2. إحالة الوكيل بإنذار مصدر ← إجراء «إحالة إلى المرشد» **واحد** ← صندوق المرشدة ←
   استلام.
3. بصمة المستند ثابتة بعد الإحالة والاستلام، ولقطة الإحالة 5 مقابل الحالي 3 بعد
   اعتماد عذر.
4. ملف الطالب الموحد: الأعذار والإنذارات والإجراءات والمستندات والإحالات في جلسة
   واحدة.
5. عزل: مديرة مدرسة أخرى — 404 في الوحدات الأربع وقوائم صفر.

### خللان في تصميم الاختبارات كشفتهما القاعدة النظيفة (البند 65)

1. **`device-roster-sync` كان يخرّج «أول طالب نشط» في المدرسة.** على قاعدة متراكمة
   كان يصادف طالبًا مهملًا؛ على قاعدة نظيفة خرّج **طالب التحليلات** فسقط
   `morning.spec` الذي يبحث عنه. الإصلاح: الاختبار صار **يستورد ملفه الخاص
   (‏noor-12) ويخرّج طالبه هو** — وزمنه هبط من 2.2 دقيقة إلى **10.7 ثانية** لأن
   سجله لم يعد يشمل كل طلاب المدرسة.
2. **اعتماد على بيانات متراكمة**: أعيد بناء البيئة كاملة (`down -v` ثم
   `build --no-cache` ثم `up`) فلم يبق أي أثر لـ«492 طالبًا» من تشغيلات سابقة،
   وكل الاختبارات مرّت على قاعدة بكر.

## PDF verification after merge

تحقق فعلي بعد الدمج داخل صورة **التشغيل** (لا صورة البناء): WeasyPrint 69 يعمل،
6 خطوط عربية مثبتة، ومستند A4 عربي RTL يُنتج ونصه **مستخرج فعليًا بـpypdf**.
وE2E ينزّل مستند الإنذار مرتين ويقارن الحجم والبصمة — متطابقان.

## Fresh migration

قاعدة `xmansx` أُنشئت من الصفر (‏`down -v`): `migrate` من `0001` حتى آخر هجرة
PASS، ثم قاعدة إضافية `xmansx_fresh1213` للتحقق المستقل PASS، و
`makemigrations --check` → "No changes detected" في كلتيهما.

## Docker

`docker compose build --no-cache backend` نجح، ثم `up` بالخدمات الست:
backend/postgres/redis/worker **healthy**، وbeat/frontend يعملان. مكتبات PDF
والخطوط موجودة في صورة التشغيل نفسها (تحقق داخل الحاوية العاملة).

## Performance / Query counts

| القياس | النتيجة |
|---|---|
| قائمة المستندات (25) | 9ms — **استعلام واحد** |
| قائمة الإجراءات (25) | 16ms — **استعلام واحد** |
| قائمة الإحالات (25) | 17ms — 4 استعلامات ثابتة (لا تتبع عدد الصفوف) |
| ملخص ملف الطالب | 6ms — **استعلام واحد** |

لا N+1 بعد إضافة التبويبات الجديدة؛ كل تبويب طلب كسول مستقل فلا ينفجر عدد
الاستعلامات بفتح الملف. قياسات م12 الثقيلة (توليد PDF) لم يمسها الدمج فلم تُعد.

## OpenAPI

`spectacular --validate` exit **0**. التحذيرات القديمة (9 تحذيرات / 298 خطأ مخطط
قديمة) **غير حاجزة** كما هو موثق في OPENAPI.md، ولا مسار جديد من م12/م13 يظهر
فيها.

## Known limitations

- توليد المستندات متزامن داخل الطلب (تقرير 500 صف ≈ 6 ثوانٍ) — النموذج يدعم
  `PENDING` فنقله إلى Celery تغيير محصور.
- لا شعار مدرسة داخل المستندات، ولا توقيع رقمي ولا ختم.
- الإجراء الناتج عن الإحالة بلا FK إليها (يحمل رقمها في الملاحظة): تفادي ازدواج
  مصدر الحقيقة وتعقيد ترتيب الحذف — قرار موثق.
- تقارير المواظبة لا تعرض الإحالات ولا الإرشاد (م14).

## Technical debt

- نقل رسم PDF إلى Celery مع بث الحالة للواجهة.
- تخزين كائني (S3) بروابط موقّعة قصيرة بدل نظام الملفات المحلي.
- أنواع إنذار إضافية (`PERIOD_LATE_OCCURRENCES`) جاهزة بنيويًا وغير منفذة.
- تحذيرات OpenAPI القديمة (غير حاجزة).

## Physical device adapter status

`PENDING REAL DEVICE` — لم يتغير بالدمج: محوّل المحاكاة (SIMULATOR) هو المسار
المختبَر، ومحوّل الجهاز الفعلي يحتاج جهازًا حقيقيًا للتحقق.

## Git status

فرع `integration/phase-12-13` بعد الالتزام: **شجرة نظيفة**، ونقطة الحماية
`rescue/pre-phase-12-13-integration` قائمة، وفرعا المرحلتين 12 و13 كما هما بلا أي
إعادة كتابة. **لا Push** (ولا remote مضبوط أصلًا).

## Final Phase 14 readiness decision

**READY** — كل معايير البند 95 محققة بتشغيل فعلي. الأساس الذي تحتاجه المرحلة 14
(بوابة المرشد وخطط المتابعة) قائم: الإحالات بحالاتها وخطها الزمني، وسجل الإجراءات
كدليل على ما جُرِّب، والمستندات كسجل رسمي ثابت. **متوقف بانتظار الاعتماد الرسمي.**
